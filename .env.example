# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/supervisor
DB_HOST=localhost
DB_PORT=5432
DB_NAME=supervisor
DB_USER=supervisor_user
DB_PASSWORD=change_me

# Secrets Encryption
SECRETS_ENCRYPTION_KEY=generate_with_openssl_rand_hex_32

# MCP Server Configuration
MCP_PORT=3000
MCP_HOST=localhost

# Anthropic API (for Claude SDK sessions)
ANTHROPIC_API_KEY=sk-ant-...

# Cloudflare (stored in secrets, but needed for bootstrap)
CLOUDFLARE_API_TOKEN=your_cloudflare_token
CLOUDFLARE_ZONE_ID=your_zone_id
CLOUDFLARE_TUNNEL_ID=your_tunnel_id

# Google Cloud (service account keys stored in secrets)
GCLOUD_PROJECT_ID=your_project_id

# Environment
NODE_ENV=development
LOG_LEVEL=info
